[<RequireQualifiedAccess>]
module Style

let color = {|
   moneyIn = "var(--ins-color)"
   moneyOut = "var(--del-color)"
   alert = "var(--del-color)"
   balance = "var(--primary)"
   primary = "var(--primary)"
   secondary = "var(--secondary)"
|}
